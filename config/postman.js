exports.default = {
  postman: api => ({
    // ---------------------------------------------------------------------
    // Allow to enable or disable Postman service
    // ---------------------------------------------------------------------
    enabled: false,

    // ---------------------------------------------------------------------
    // Configurations to connect with the SMTP server
    // ---------------------------------------------------------------------
    server: {
      // ---------------------------------------------------------------------
      // SMTP hostname
      // ---------------------------------------------------------------------
      host: "smtp.example.com",

      // ---------------------------------------------------------------------
      // Port where the SMTP sever is listening on
      // ---------------------------------------------------------------------
      port: 587,

      // ---------------------------------------------------------------------
      // Used to enforce a secure connection with the SMTP server
      // ---------------------------------------------------------------------  
      secure: false,

      // ---------------------------------------------------------------------
      // Authentication configurations to connected with the SMTP server
      // ---------------------------------------------------------------------
      auth: {
        // ---------------------------------------------------------------------
        // SMTP username
        // ---------------------------------------------------------------------
        user: "some-user@example.com",

        // ---------------------------------------------------------------------
        // Password for the SMTP user
        // ---------------------------------------------------------------------
        pass: "this-is-an-example-pass"
      }
    },

    // ---------------------------------------------------------------------
    // Default configs when the developer is sending the email
    // ---------------------------------------------------------------------
    mail: {
      // ---------------------------------------------------------------------
      // Default address when sending a email
      // ---------------------------------------------------------------------
      form: '"Jack Foo" <foo@example.com>',
    }
  })
};
